{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run site locally",
            "type": "shell",
            "command": "hugo server --bind 0.0.0.0",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}